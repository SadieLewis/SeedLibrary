@page
@using Microsoft.AspNetCore.Http.Features
@using SeedLibrary.Models
@model SeedLibrary.Pages.Plants.IndexModel

@{
    ViewData["Title"] = "Donations";
}

<h2>Plants</h2>

<p>
    <a asp-page="Create" class="btn btn-success mb-3">Create New Plant</a>
    <a asp-page="/Varieties/Index" class="btn btn-warning mb-3">View Plant Varieties</a>
    <a asp-page="/Varieties/Create" class="btn btn-warning mb-3">Create New Plant Variety</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                Plant Common Name
            </th>
            <th>
                Varieties
            </th>
            <th>
                Number of Varieties
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CommonName) {
        <tr>
            <td>
                @item.Name
            </td>
            <td>
                @string.Join(", ", item.Varieties.Select(v => v.VarietyName))
            </td>
            <td>
                @item.Varieties.Count
            </td>
        </tr>
}
    </tbody>
</table>